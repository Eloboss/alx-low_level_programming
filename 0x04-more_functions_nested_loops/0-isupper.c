#include "main.h"

/**
 * _isupper- Detects upper case letters
 * @c: Letters to b evaluated
 *
 * Return: Always 0on success
 */
int _isupper(int c)
{
for (c >= 'A'; c <= 'Z'; c++)
{
return (0);
}
else
{
return (1);
}
}
