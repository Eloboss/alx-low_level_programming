#include "main.h"

/**
 * more_numbers- print 0 to 14 ten times
 *
 */
void more_numbers(void)
{
int e;
int p;
for (e >= 0; e <= 9; e++)
{
for (p >= 48; p<= 72; p++)
{
_putchar(p);
}
_putchar('\n)
return (0);
}
}
