#include "main.h"
/**
 * _abs- Prints absolute value of a number
 * @n: Number to b evaluated
 *
 * Return: Always 0 on success
 */
int _abs(int n)
{
if (n >= 1)
{
return (n);
}
else
{
n *= -1;
return (n);
}
}
