#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main- Entry point
 *
 * Return: Sign of numbers
 */
int main(void)
{
int e;
char p;
for (e = 0; e <= 9; e++)
{
putchar(e);
}
for (p = 'a'; p <= 'f'; p++)
{
putchar(p);
}
putchar('\n');
return (0);
}
