#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main- Entry point
 *
 * Return: Sign of numbers
 */
int main(void)
{
char elo;
for (elo = 'a'; elo <= 'z'; elo++)
{
if (elo = 'q' || elo = 'e')
{
continue;
putchar(elo);
}
}
putchar('\n');
return (0);
}
